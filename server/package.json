{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./src/server.js",
    "dev": "nodemon ./src/server.js",
    "express-dev": "nodemon ./src/server.js",
    "migrate-latest": "knex migrate:latest",
    "migrate-down": "",
    "seed-data": "knex seed:run",
    "start-both": "npm run start && (cd ../client && npm run build)",
    "build-both": "npm install && (cd ../client && npm install)"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "connect-session-knex": "^3.0.1",
    "cors": "^2.8.5",
    "dayjs": "^1.11.10",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "knex": "^3.0.1",
    "nodemon": "^3.0.1",
    "pg": "^8.11.3"
  }
}
