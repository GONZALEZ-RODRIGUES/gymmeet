{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./src/server.js",
    "dev": "nodemon ./src/server.js",
    "express-dev": "nodemon ./src/server.js",
    "migrate-latest": "knex migrate:latest",
    "migrate-down": "",
    "seed-data": "knex seed:run",
    "start-both": "npm start & (cd ../client && npm run dev)",
    "build-both": "npm install & (cd ../client && npm install)"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "knex": "^3.0.1",
    "nodemon": "^3.0.1",
    "pg": "^8.11.3"
  }
}
